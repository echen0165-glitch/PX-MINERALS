import '/app/favicon.js';

const form=document.querySelector('#register-form'),error=document.querySelector('#register-error');const referralFromLink=new URLSearchParams(window.location.search).get('ref');if(referralFromLink)document.querySelector('#referralCode').value=referralFromLink;form.addEventListener('submit',async(event)=>{event.preventDefault();error.textContent='';const value=id=>document.querySelector(`#${id}`).value.trim();const response=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({firstName:value('firstName'),lastName:value('lastName'),username:value('username'),birthDate:value('birthDate'),waveNumber:value('waveNumber'),email:value('email'),password:document.querySelector('#password').value,passwordConfirmation:document.querySelector('#passwordConfirmation').value,acceptedTerms:document.querySelector('#terms').checked,referralCode:value('referralCode')||undefined,avatarKey:value('avatarKey')})});const payload=await response.json().catch(()=>({}));if(!response.ok){error.textContent=payload.error?.message??'Inscription impossible.';return}sessionStorage.setItem('pxVerificationEmail',value('email'));window.location.replace('/app/verify-email.html');});
